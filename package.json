{
  "name": "danvers-karakeep-extension",
  "private": true,
  "description": "Save the current page to Karakeep from an inline Iceraven-friendly overlay.",
  "scripts": {
    "lint": "web-ext lint",
    "build": "web-ext build",
    "sign": "web-ext sign --channel=unlisted",
    "start": "web-ext run"
  },
  "devDependencies": {
    "web-ext": "^10.4.0"
  }
}
